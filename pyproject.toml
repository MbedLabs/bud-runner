[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bud_runner"
version = "0.3.6"
description = "CLI tool for test execution and Bud Test Management Platform integration"
readme = "README.md"
license = {text = "AGPL-3.0-or-later"}
authors = [
    {name = "EmbedLabs", email = "dev@embedlabs.de"}
]
maintainers = [
    {name = "EmbedLabs", email = "dev@embedlabs.de"}
]
keywords = [
    "testing",
    "automation",
    "ci-cd",
    "runner",
    "junit",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Testing",
    "Topic :: Software Development :: Quality Assurance",
]
requires-python = ">=3.9"
dependencies = [
    "typer>=0.9.0",
    "requests>=2.28.0",
    "rich>=13.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "mypy>=1.0.0",
]

[project.scripts]
bud-runner = "bud_runner.cli:main"

[project.urls]
Homepage = "https://embedlabs.de"
Documentation = "https://embedlabs.de/docs"
Repository = "https://github.com/MbedLabs/bud-runner"
Issues = "https://github.com/MbedLabs/bud-runner/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["bud_runner*"]

[tool.black]
line-length = 100
target-version = ["py39", "py310", "py311", "py312"]

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-ra -q"
testpaths = ["tests"]
